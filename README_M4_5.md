# M4.5.1 Repository

Copy the package contents into the repository root.

Run:

```bash
python3 -m pytest -q
```

Create the review package after adding the M4.5 milestone configuration supplied in `tools/review_milestones_m4_5_snippet.json`.


## Patch verification

This release fixes M4.5-F-001 through M4.5-F-003. UTC timestamps are serialized with fixed-width microseconds.
