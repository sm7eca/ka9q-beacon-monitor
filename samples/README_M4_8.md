# M4.8 Web UI

Copy the supplied files into the repository preserving paths. The UI is a read-only FastAPI application that consumes the M4.7 REST API.

Run tests with:

```bash
python3 -m pytest -q
```

Create the review package with:

```bash
python3 tools/create_review_package.py M4.8
```
